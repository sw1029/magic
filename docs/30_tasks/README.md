# Tasks

Task documents live in this directory and are validated against `docs/20_queue/work-queue.md`.

Each task document should include frontmatter entries for `id`, `status`, `depends_on`, and `blocks`.
